import useCurToken from "./useCurToken";

// 获取 interest

// 仅在layout 中调用一次，无需多处使用，需要使用相关数据，请使用其他方式获取

const useOpenInterest = () => {

  // 
  const { curToken } = useCurToken();
  const positions = 





  
};
export default useOpenInterest;
